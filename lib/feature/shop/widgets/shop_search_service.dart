import 'package:flutter/material.dart';

class ShopSearchService extends StatelessWidget {
  const ShopSearchService({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
